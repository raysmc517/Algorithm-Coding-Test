n = int(input()) # 모험가 인원 입력
b = list(map(int, input().split())) #공포도 입력
b.sort()
g = 0 # 총 그룹의 수
c = 0 # 현재 그룹에 포합된 모험가의 수
for i in b: #공포도를 낮은 것부터 하나씩 확인하며
    c += 1  #현재 그룹에 해당 모험가를 포함시키기
    if c >= i:#현재 그룹에 포함된 모험가의 수가 현재 공포도 이상? --> 그룹 결성
        g += 1 # 총 그룹 수 증가
        c = 0   # 모험가의 수 초기화

print(g)
